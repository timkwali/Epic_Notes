dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warning: this re
        maven { url 'https://jitpack.io' }// pository is going to shut down soon
    }
}
rootProject.name = "Epic Notes"
include ':app'
